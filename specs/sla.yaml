sla: 1.0.0
context:
  id: simple-api
  api:
    $ref: ./simple_api_oas.yaml
metrics:
  requests:
    type: "int64"
    description: "Number of requests"
plans:
  standardPlan:
    rates: # TODO: or quotas ?
      /once-per-second-endpoint:
        get:
          requests: # TODO: why is this an array?
            - max: 1
              period: second
      /twice-per-minute-endpoint:
        get:
          requests:
            - max: 2
              period: minute
      #/open-endpoint: # TODO: paths w/o limits?
      #  get:
      #    requests:
      #      - max:
      #        period:
