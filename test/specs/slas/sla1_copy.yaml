sla4oai: 1.0.0
context:
  id: 1perSecond
  type: agreement
  api:
    $ref: ./simple_api_oas.yaml
metrics:
  requests:
    type: integer
    format: int64
    description: "Number of requests"
rates:
  /once-per-second-endpoint:
    get:
      requests:
        - max: 1
          period: second
