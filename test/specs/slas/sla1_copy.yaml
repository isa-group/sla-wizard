sla: 1.0.0 # TODO
context:
  id: 1perSecond
  type: instance
  api:
    $ref: ./simple_api_oas.yaml
metrics:
  requests:
    type: integer
    format: int64
    description: "Number of requests"
rates:
  /once-per-second-endpoint:
    get:
      requests:
        - max: 1
          period: second
