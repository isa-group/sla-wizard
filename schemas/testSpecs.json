{
    "id": "testSpecsSchema",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "SLA Wizard Test Specs Schema",
    "type": "object",
    "required": [
        "authLocation",
        "iterations"
    ],"properties": {
        "authLocation": {
            "description": "Where to look for the authentication token.",
            "enum": [
                "header",
                "query",
                "url"
            ],
            "title": "authLocation",
            "type": "string"
        },
        "iterations": {
            "description": "Units of time to perform the test. If a rate limiting is 'per minute' and this is set to 2, then that specific rate limiting testing will run for 2 minutes.",
            "title": "iterations",
            "type": "number"
        }
    },
    "additionalProperties": false
}